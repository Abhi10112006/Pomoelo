package com.example.ui.theme

import androidx.compose.ui.text.font.Font
import androidx.compose.ui.text.font.FontFamily
import androidx.compose.ui.text.font.FontWeight
import com.example.R

val AppFontFamily = FontFamily(
    Font(resId = R.font.comic_neue_regular, weight = FontWeight.Normal),
    Font(resId = R.font.comic_neue_bold, weight = FontWeight.Bold)
)

val CursiveFontFamily = FontFamily(
    Font(resId = R.font.comic_neue_regular, weight = FontWeight.Normal),
    Font(resId = R.font.comic_neue_bold, weight = FontWeight.Bold)
)

val MonospaceFontFamily = FontFamily(
    Font(resId = R.font.special_elite_regular, weight = FontWeight.Normal)
)




